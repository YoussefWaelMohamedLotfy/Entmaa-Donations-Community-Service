<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Entmaa" parent="Platform.Theme.Entmaa">

        <!-- Status bar -->
        <item name="android:statusBarColor">?attr/colorSurface</item>

        <!-- Buttons -->
        <item name="materialButtonStyle">@style/Entmaa.Button</item>

        <!-- TextInputLayout -->
        <item name="textInputStyle">@style/Entmaa.TextInputLayout.OutlinedBox</item>

        <!-- Chips  -->
        <item name="chipStyle">@style/Entmaa.Chip</item>


        <item name="bottomSheetDialogTheme">@style/Entmaa.BottomSheetDialog</item>

        <item name="fontFamily">@font/nunito_bold</item>
    </style>

</resources>